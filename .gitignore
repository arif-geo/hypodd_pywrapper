# Run directories are working space, not source. `run_dir` in a run config points wherever you
# want (slate, scratch, here) — nothing in the repo depends on runs/ existing.
runs/

# Legacy SLURM output
legacy/logs/

# Build and cache artifacts
__pycache__/
*.egg-info/
.pytest_cache/
*.pyc

.DS_Store
examples/*/run/
