# hypodd_pywrapper
Simple python wrapper for hypoDD where you just input simple csv files and run a python script to 1. compile hypoDD >> 2. convert csvs into appropriate format >> 3. run hypoDD (*.inp must be changed manually) >> 4. return a csv relocated catalog
